<li>
  <%= link_to park.parkname, park %>
  |
  <span class=<%= colorize(park.evaluation) %>><%= display_grade(park.evaluation) %></span>
  |
  <%= "#{park.acreage} acres" %>

  <% if signed_in_user? %>
    |
    <%= favorite_link(park) %>
  <% end %>
</li>
